<header>
	<nav class="navbar navbar-expand-lg navbar-light bg-light">

		<a class="navbar-brand docs-creator" href="#">BoomerGame</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarColor03">
			<ul class="navbar-nav mr-auto">
				<li class="nav-item active">
					<a class="nav-link docs-creator" href="{{ path("home") }}">

						<i class="fas fa-home">Accueil</i>
					</a>

				</li>
				<li class="nav-item">
					<a class="nav-link docs-creator" href="{{ path("article") }}">

						<i class="fas fa-newspaper">Jeux</i>
					</a>

				</li>
			</ul>


			<ul class="navbar-nav">
				{% if user.isConnected == false %}
					<li class="nav-item">
						<a class="nav-link docs-creator" href="">
							<i class="fas fa-home">Connexion</i>
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link docs-creator" href="#">
							<i class="fas fa-newspaper">Inscription</i>
						</a>
					</li>


				{% else %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle docs-creator" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ user.firstname }}
							{{ user.lastname }}</a>
						<div class="dropdown-menu">
							{% if user.role == "admin" %}
								<a class="dropdown-item docs-creator" href="{{ path("account") }}">Espace Admin</a>
							{% endif %}
							<div class="dropdown-divider"></div>
							<a class="dropdown-item docs-creator" href="#">Déconnexion</a>
						</div>
					</li>
				{% endif %}
			</ul>

		</div>

	</nav>
</header>
